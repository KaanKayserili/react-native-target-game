import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const LastGuess = ({ text }) => {
    return (
        <View>
            <Text>{text}</Text>
        </View>
    )
}

export default LastGuess

const styles = StyleSheet.create({})